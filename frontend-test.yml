version: '3.8'

services:
  lol_dict:
    depends_on:
      lol_dict_db:
        condition: service_healthy
    build: 
      context: .
      dockerfile: Dockerfile.testFrontend
    container_name: lol_dict_test_frontend
    ports:
      - 5000:5000
    tty: true
    stdin_open: true
  lol_dict_db:
    build: 
      context: .
      dockerfile: ./db/Dockerfile
    container_name: lol_dict_db
    volumes:
      - ./db/mysql:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: abichan99
      TZ: Asia/Tokyo
    restart: always
    healthcheck:
      test: ["CMD", 'mysqladmin', 'ping', '-h', 'localhost', '-u', 'root', '-p$$MYSQL_ROOT_PASSWORD' ]
      timeout: 20s
      retries: 10